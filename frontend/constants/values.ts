// Timer Screen constant values
const DEFAULT_INITIAL_TIME = 30 * 60;
const MAX_TIME_SECONDS = 3 * 60 * 60;
const MIN_TIME_SECONDS = 5;

const values = {
  DEFAULT_INITIAL_TIME,
  MAX_TIME_SECONDS,
  MIN_TIME_SECONDS
}

export default values;

// Consider splitting each catergory into named exports